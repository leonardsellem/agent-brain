# Changesets

Use changesets to prepare reviewed version and changelog updates before a deliberate GitHub Release or version tag publishes to npm.

```bash
npm run changeset
```

The release PR workflow updates package versions and changelog state. It does not publish to npm.
