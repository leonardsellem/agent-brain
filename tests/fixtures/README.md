# Test Fixtures

Fixtures model Claude Code and Codex app roots, import sources, and generated Agent Brain repos. Tests must use fixture paths or in-memory filesystem ports, not the real home directory.
